# Copy to .env.local. This file is gitignored and is used ONLY by local admin /
# seed scripts (e.g. scripts/seedSupabase.mjs). Never reference these vars from
# app code — they must not end up in the Expo bundle.

SUPABASE_URL=https://xeppwiugjvnigttpseev.supabase.co
SUPABASE_SECRET_KEY=sb_secret_xxx
